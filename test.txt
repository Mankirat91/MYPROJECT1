1)Write a Python program to append a new item to the end of the array(T)
2)Write a program to check if number is prime in python(F)
3)What is difference between cookie and session (T) create at server store at client
4)Write a function to add and remove element to array in python(T)
5)Which of the following is invalid statement(F)
    a)x y z = 1000
    b)a,b,x=45
    c)xyz=56
    d)a_b_c=300
6)<div id="inner"><h3>Hello</h3></div>
    Write a program in javascript to get "Hello" (F)
7)var a=[{name:"mankirat",class:"10",age:20},{name:"charan",class:"12",age:28}]
    a.find(c=>c.name == 'charan').class
    write javascript program to find class of charan(T)
8)what will be output of this function(F)
    def hello():
  name = str(input("Enter your name: "))
  if name:
    print ("Hello " + str(name))
  else:
    print("Hello World") 
  return 

hello()

9)Create a Dog class in python , init color , legs and tail variables with function bark() and run() . Print "Barking" and "Running" inside functions(F)

10) what will be output of following function(T)
 my_list = [1, 2, 3, 4, 5]
 print(len(my_list)) 

#cookies(http request only in browser,specific time) localStorage(browser end , never destroy once set)(data security)
#mutiple file upload using html and javascript
